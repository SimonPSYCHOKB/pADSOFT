package GUI;

import javax.swing.*;

import Test.*;

public class UIQuestion extends JPanel{

	private static final long serialVersionUID = 1L;
	private JPanel root;
	
	private UIQuestion(Question question) {
		root = new JPanel();
		root.setLayout(new BoxLayout(root, BoxLayout.PAGE_AXIS));
		
		JLabel wording = new JLabel(question.getQuestion());
		root.add(wording);
		root.setVisible(true);
	}
	
	public UIQuestion(SingleAnswer question){
		this((Question) question);
		
		for(String option : question.getOptions()){
			JRadioButton radio = new JRadioButton(option);
			root.add(radio);
		}
	}
	
	public UIQuestion(MultipleAnswer question){
		this((Question) question);
		
		for(String option : question.getOptions()){
			JCheckBox radio = new JCheckBox(option);
			root.add(radio);
		}
	}
	
	public UIQuestion(FreeText question){
		this((Question) question);
		
		JTextField answer = new JTextField(10);
		root.add(answer);
	}
	
	public UIQuestion(TrueFalse question){
		this((Question) question);
		
		JTextField answer = new JTextField(10);
		root.add(answer);
	}

}
