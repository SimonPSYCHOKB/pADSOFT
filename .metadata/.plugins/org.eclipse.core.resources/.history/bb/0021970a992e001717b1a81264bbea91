package GUI;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import Application.Application;

public class CreateUnitController implements ActionListener{

	private Application model;
	private UICourse view;
	
	public CreateUnitController(Application model, UICourse view){
		this.model = model;
		this.view = view;
	}

	@Override
	public void actionPerformed(ActionEvent arg0) {
		CreateUnit frame = (CreateUnit) arg0.getSource();
		model.addUnitToCourse(new Unit, c)
		
	}
}
