package GUI;

import java.util.*;

import javax.swing.*;

import Test.*;

public class UIQuestion extends JPanel{

	private static final long serialVersionUID = 1L;
	private JPanel root;
	
	public UIQuestion(Question question) {
		root = new JPanel();
		root.setLayout(new BoxLayout(root, BoxLayout.PAGE_AXIS));
		
		JLabel wording = new JLabel(question.getQuestion());
		root.add(wording);
		root.setVisible(true);
		
		if(question instanceof SingleAnswer)
			this((SingleAnswer) question);
	}
	
	private List<JRadioButton> SingleAnswer(SingleAnswer question){
		List<JRadioButton> buttons = new ArrayList<JRadioButton>();
		for(String option : question.getOptions()){
			JRadioButton radio = new JRadioButton(option);
			buttons.add(radio);
		}
		return buttons;
	}
	
	private UIQuestion(MultipleAnswer question){
		for(String option : question.getOptions()){
			JCheckBox radio = new JCheckBox(option);
			root.add(radio);
		}
	}
	
	private UIQuestion(FreeText question){
		JTextField answer = new JTextField(10);
		root.add(answer);
	}
	
	private UIQuestion(TrueFalse question){
		JTextField answer = new JTextField(10);
		root.add(answer);
	}

}
